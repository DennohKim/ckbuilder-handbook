node_modules/
dist/

# Regenerated by `npm run sync` before every dev/build/test run: the devnet's
# genesis out-points and the escrow's deployed code hash both change whenever
# the chain or the script is redeployed.
src/ckb/devnet-known-scripts.json
src/ckb/escrow-script.json
